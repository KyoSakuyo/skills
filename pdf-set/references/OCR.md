CRITICAL: 
时刻记住你是的任务是一页一页地输出文档，不要把任何上页或下页的内容混到该页去。每一页都要生成，和图片一一对应。注意首行缩进。
你analyze完图片200.jpg-205.jpg后，把200.jpg的内容只能输出在200.md中，不能输出到201.md或者199.md中。

当任务开始时，随机抽样检查用户的pdf图片中的注释类型是否是普通的尾注或脚注──如果是特殊的注释类型（像是3页正文1页注释）则建议用户删除注释。

## 输入/输出

- 你要读取「图片」文件夹中的图片，并识别图片中的文字（列计划的时候，一次Batch最多**五张**图片，不要给一个batch中列超过5张以上的图片），（analyze完一张图片后，你就需要think一次，找到该图片的**边界**，五张图都think完之后再一起输出文件）在「OCR结果」的文件夹内（如果没有就新建），输出「原图序号.md」的文档。
如果该图片没有正文，你就生成「原图序号.md」空文档。但是每一张图片都要有和图片内容一一对应的文档。

注意：在每一次Think的过程中，你需要注意并识别出该图片的正文部分的第1字符和最后1字符──作为后续生成「原图序号.md」文件时的边界──你必须尊重原图的文字边界！！如果你识别到200.jpg的正文的开头边界是「伸」─这就是「200.md」文档的开头， **绝对不允许**随便猜测文意，把199.jpg的结尾的「身体的延」或者再前面几个词给加进去。同理，如果一个文档的结尾边界是「、」或者「，」或者「：」，你可以非常明显地判断出它们是被分割开的不完整的句子──但是，一定注意：**什么都别动**，不要改写它的内容，上页正文内容是放在上页的md文件里的，下页正文内容是下页的md文件里的！！你必须尊重原文，严格符合边界！！

如果该页结尾断句没断完，不要**补充**或改写原文。**不允许从下一页调取内容来补写上一页**。这很重要！！识别到什么就输出什么。

- 严禁使用浏览器来进行OCR工序；或者tesseract进行批量 OCR 生成；直接用模型视觉能力识别图片文字。
- 因为你都是按照顺序生成的，所以不需要在任务开始前、进行中或者任务完成后用ls命令或者Get-ChildItem命令等命令检查文件列表，直接生成就好

## 阶段 1：清理（必须删除）
- 页码（包括页眉/页脚的页码）。
- 边码 / 边栏编号。
- 每页页眉页脚中重复出现的章节标题名。

## 阶段 2：OCR 识别规则
1. **字符忠实**：简体保持简体，繁体保持繁体；不要做简繁转换。不要变成文言文。更不要给原文换语言，不要把原书中的「他」换成「he」，把「和」换成「and」，把「的」换成「of」或者「의 」，不要把「了」换成「ました」…… 与此同时，保持原文的完整性，不可以增减掉和改写任何该页正文的内容。
2. **版式剔除**：删除页眉/页脚页码、边码/边栏数字、每页重复的章节标题。
3. **复杂图形处理**：
   - 简单数学/希腊字母可用 LaTeX，例如：`($S(\\cancel{A})$)`, `\\sigma`, `$p^o$`。
   - 当复杂公式/图示/配图无法文本化时，**跳过内容**，但在对应位置插入 `🀄️，该页页码` 标记。（要注意，页码应该是在图片中出现的页眉或者页尾的页码。）
   - **禁止**使用 Markdown 图片语法；只用 `🀄️，该页页码`表示图片。
   - 注意，`🀄️，该页页码`只是提示符，提示该位置有图片，以便我在正文中手动插入图片。

## 阶段 3：排版规则
1. **分段**：段落间用**两个换行符制作空行**；而同一个段落内不要空行──也不许用一个换行符换行。

   注意：一页的内容输出的结尾不允许留空行

   错误示例：

   ```
     恶与罪在暴民心理中的吸引力早已有之。暴民一向会欢迎“暴
       
   ```

   需要以：

   ```
     恶与罪在暴民心理中的吸引力早已有之。暴民一向会欢迎“暴
   ```

   的形式直接结束该页。

2. **标题**：章节标题或小标题一律用两个井号的二级标题 `##`；禁止使用一级或三级标题。

3. **文本样式**：所有加粗/斜体/特殊样式一律还原为纯文本。

4. **首行缩进**：首先判断是不是正文部分；如果是：你需要识别这是不是一个段落的段首（段首通常会在图片中有首行缩进的效果），如果是：新段落**段首**加 两个空格字符的首行缩进；注意，只有段首需要，而不是给每一行都加首行缩进。若判断该段为上一页结束处断开的段落之续接部分（续接部分通常会在图片中**没有**首行缩进的效果，而且句意通常都没有说完，不完整，你尤其需要在一张图片的开头部分特别注意是不是续接部分），则不要加首行缩进。

如果是对话体和诗歌，你要格外注意首行缩进和分段，给每个人的对话中间都要留空行，每个新行都需要缩进。

如果我给了你「使用你的pdf-set的skill，处理xx书籍的201-267文件」类似的指令，你需要分析一下200文件的文本内容去判断201是不是上一页结束处断开的段落之续接部分

示例：

```
……上一段落结尾
（空行）
  下一段落开头
```

## 阶段 4：脚注转移（仅页尾脚注footnote）

如果用户要求了删除注释，则不做脚注转移，把所有注释删除。

- 唯一需要你用拓展markdown的格式做转移的情况，是使用`<sup>【】</sup>`表示的上标格式把**该页页尾**的脚注注释转移到正文中。(仅有该页页尾需要！！)一般情况下，正文内的文内注号和页尾的脚注编号是有着一一对应关系的。你把每一个脚注编号下的注释文本转移到文内注号后，以`<sup>【】</sup>`的格式包裹住，然后把文内注号删掉。注意：注释中不可以分段；如果原图中的注释是分段有空行的，你需要把空行去掉。

举例：

```
1938-1950年的理論，中的症狀端賴著拉岡最後在1950年清楚標呼為「伊底帕斯主義的社會情況」，14-2 其論點後來成為主體成熟理論的軸心，主要圍繞在父親原相，這原相的結構化價值，跟家庭中的父親所具的社會價值有直接的關聯；其次，與家庭是否整合於社會也息息相關。
……
14-2 Op. cit., p.136.15-1 馬林諾斯基（B. Malinowski 1884-1942）人類學家，功能主義奠基者之一。他對性行為的研究讓他成為這領域的先鋒者，主要是研究超布連（Trobriand）群島的美拉尼西亞人。

特別參考《性以及它在原始社會的壓抑》（La sexualité et sa repression dans les sociétés primi-tives, Paris, Payot, 1932)。
```

你帮我处理为：

```
1938-1950年的理論，中的症狀端賴著拉岡最後在1950年清楚標呼為「伊底帕斯主義的社會情況」，<sup>【Op. cit., p.136.15-1 馬林諾斯基（B. Malinowski 1884-1942）人類學家，功能主義奠基者之一。他對性行為的研究讓他成為這領域的先鋒者，主要是研究超布連（Trobriand）群島的美拉尼西亞人。
特別參考《性以及它在原始社會的壓抑》（La sexualité et sa repression dans les sociétés primi-tives, Paris, Payot, 1932)。】</sup>其論點後來成為主體成熟理論的軸心，主要圍繞在父親原相，這原相的結構化價值，跟家庭中的父親所具的社會價值有直接的關聯；其次，與家庭是否整合於社會也息息相關。
```

   注意：注释中不可以分段；即使原图中的注释是分段的，你就把它们合并为一段。

2. 如果遇到注释中一条注释的末尾是「接下页」或者是一个「-」符号，而且你判断话没说完的情况的情况，通常情况下这是一个没有写完的注释，你需要在注释的最末尾加一个🀄️的符号；它的下一页会有一条没有出现标号的另一个看起来话没说完的注释（可能会有「续上页」的文字提示；或者没有任何的注号），你需要把它放在该页的最前端，同样也需要加一个🀄️的符号。
3. 若正文中的注号对应**章节末/书末**的尾注Endnotes：
   - 不删除文内注号；
   - 将该注号改为 <sup>【原注号（无注释内容）】</sup> 形式；
   - 章节末/书末尾注注释内容保持原样，不做内嵌或清理。
4. 不同注释之间保留空行（用于章节末/书末注释列表）。

## 阶段 5：输出检查
- 段落与标题格式符合上述规则；
- 无 Markdown 图片语法。
- 没有出现莫名其妙的原文没有的，像是'he'和'and'或'of'或'의'或'or'这样的字符。
- 检查没有把任何上页或下页的内容混到你处理的该页去。