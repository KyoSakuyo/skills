CRITICAL: 按顺序执行以下阶段，勿跳步。

## 输入/输出
- 输入：`OCR结果/` 文件夹内的 `序号.md` 文件（每个序号对应书的一页）。
- 输出：合并为单一文件 `0.粗全文.md`，写入 `合并结果/`（不存在就创建）。

## 阶段 1：确认顺序
1. 读取用户给定的页码顺序与文件名列表。
2. 严格按该顺序合并；不要自行重排或补页。

## 阶段 2：合并规则
- 仅做拼接，不做任何排版、清理、纠错或格式调整。
- 保持每个页文件内容原样；必要时用换行分隔页与页，避免内容黏连。

## 阶段 3：输出检查
- `合并结果/0.粗全文.md` 存在且内容完整。
- 合并顺序与用户提供的列表一致。
